# Simple Database Interface (sdbi)

## usage:
    - define database according to the example provided in config.toml
    - run main.py which will create table if it never existed 
    - now you can freely insert data via UI